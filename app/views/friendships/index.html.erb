<%= react_component(
  "Friendships",
  props: {
    user: @user.as_json(),
    usermatches: @usermatches.as_json(),
    userwc: @userwc,
    allusers: @allusers.as_json(),
    friends_matches: @friends_matches.as_json(),
    friends_wc: @friends_wc,
    friends: @friends,
    Favicon: "/assets/LogoO2.png",
    RetoLogo: "/assets/RetoLogo2.png"
  },
  options: {
    prerender: false
  }
) %>